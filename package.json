{
  "name": "surakshamcp-server",
  "version": "0.1.0",
  "description": "SurakshaMCP — multilingual, voice-first construction-safety MCP server (NitroStack).",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "deploy": "nitro deploy --prod"
  },
  "dependencies": {
    "@nitrostack/core": "^1.0.0",
    "dotenv": "^16.4.5",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@nitrostack/cli": "^1.0.0",
    "@types/node": "^26.1.1",
    "tsx": "^4.19.0",
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=20.18.1"
  }
}
